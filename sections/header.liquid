<header data-site-header class="site-header">
  <div class="container site-header__inner">
    <nav class="site-nav" aria-label="Navigation principale">
      {%- if section.settings.links_text != blank -%}
        {%- assign lines = section.settings.links_text | newline_to_br | split: '<br />' -%}
        {%- for raw in lines -%}
          {%- assign line = raw | strip -%}
          {%- if line == blank -%}{%- continue -%}{%- endif -%}
          {%- assign parts = line | split: '|' -%}
          {%- assign title = parts[0] | strip -%}
          {%- assign url = parts[1] | strip | default: '#' -%}
          {%- assign is_active = false -%}
          {%- if request.path == url -%}{%- assign is_active = true -%}{%- endif -%}
          <a href="{{ url }}" class="site-nav__link {% if is_active %}is-active{% endif %}">{{ title }}</a>
        {%- endfor -%}
      {%- elsif section.settings.menu != blank and section.settings.menu.links.size > 0 -%}
        {%- for link in section.settings.menu.links -%}
          <a href="{{ link.url }}"
             class="site-nav__link {% if link.active or link.child_active %}is-active{% endif %}">
            {{ link.title }}
          </a>
        {%- endfor -%}
      {%- endif -%}
    </nav>

    {%- render 'logo' -%}

    <div class="site-header__actions">
      <button type="button" class="btn btn-sm btn-secondary site-header__action" data-search-toggle
              style="border-color: var(--line-strong);">
        {{ 'header.search' | t }}
      </button>
      <a href="{% if customer %}{{ routes.account_url }}{% else %}{{ routes.account_login_url }}{% endif %}"
         class="btn btn-sm btn-secondary site-header__action"
         style="border-color: var(--line-strong);">
        {{ 'header.account' | t }}
      </a>
      <a href="{{ routes.cart_url }}"
         class="btn btn-sm site-header__action site-header__cart"
         style="background: var(--pink-deep); color: var(--ink);">
        {{ 'header.cart' | t }} · <span data-cart-count>{{ cart.item_count }}</span>
      </a>
    </div>
  </div>
</header>

<style>
  .site-header {
    position: sticky; top: 0; z-index: 50;
    background: transparent;
    border-bottom: 1px solid transparent;
    transition: background .3s, backdrop-filter .3s, border-color .3s;
  }
  .site-header[data-scrolled="true"] {
    background: rgba(255, 249, 244, 0.85);
    backdrop-filter: blur(20px) saturate(140%);
    -webkit-backdrop-filter: blur(20px) saturate(140%);
    border-bottom-color: var(--line);
  }
  [data-palette="cool"] .site-header[data-scrolled="true"] { background: rgba(246, 251, 250, 0.85); }
  [data-palette="candy"] .site-header[data-scrolled="true"] { background: rgba(255, 246, 248, 0.85); }

  .site-header__inner {
    display: flex; align-items: center; justify-content: space-between;
    height: 76px; gap: 24px;
  }
  .site-nav { display: flex; gap: 24px; font-size: 14px; font-weight: 500; white-space: nowrap; }
  .site-nav__link {
    color: var(--ink-soft);
    position: relative; padding: 4px 0;
    transition: color .2s;
  }
  .site-nav__link:hover, .site-nav__link.is-active { color: var(--ink); }
  .site-header__actions { display: flex; gap: 8px; align-items: center; }
  .site-header__cart { font-weight: 600; }

  @media (max-width: 900px) {
    .site-nav { display: none; }
    .site-header__action:not(.site-header__cart) { display: none; }
    .site-header__inner { height: 64px; }
  }
</style>

{% schema %}
{
  "name": "En-tête",
  "settings": [
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "textarea",
      "id": "links_text",
      "label": "Liens du menu (un par ligne, format \"Titre | URL\")",
      "default": "Accueil | /\nCollections | /collections\nBombes | /collections/bombes\nSoins | /collections/cremes\nLe Journal | /blogs/le-journal",
      "info": "Si rempli, ces liens remplacent le menu Shopify."
    },
    {
      "type": "link_list",
      "id": "menu",
      "label": "Menu Shopify (alternative)",
      "info": "Utilisé si le champ ci-dessus est vide."
    }
  ]
}
{% endschema %}
