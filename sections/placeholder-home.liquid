{%- comment -%} Stub d'accueil — remplacé au Stage 3 {%- endcomment -%}
<section style="padding: 120px 0; text-align: center;">
  <div class="container">
    <div class="eyebrow" style="margin-bottom: 16px;">· Thème en cours de construction</div>
    <h1 class="h-display" style="font-size: clamp(56px, 8vw, 112px); margin-bottom: 24px;">
      {{ settings.brand_name }}
    </h1>
    <p style="font-size: 18px; color: var(--ink-soft); max-width: 480px; margin: 0 auto;">
      La page d'accueil arrive au Stage 3. La barre d'annonce, l'en-tête et le pied de page arrivent au Stage 2.
    </p>
  </div>
</section>
{% schema %}
{
  "name": "Accueil — placeholder",
  "settings": []
}
{% endschema %}
